/* Напиши функцію makeOrderList, яка приймає рядок order, та повертає об'єкт, де ключами будуть типи продуктів, а значеннями - їхня кількість.

1. в замовленні кількість стоїть перед типом продукта
2. продукти в замовленні розділені комою з одним пробілом
3. якщо назва складається з декількох слів, запиши її у snake_case
4. Для порожнього рядка поверни порожній об'єкт.

Приклад: 
const order = '1 coca cola, 5 chicken nuggets, 20 egg';
const list = makeOrderList(order);

list === {
  coca_cola: 1,
  chicken_nuggets: 5,
  egg: 20,
}
*/

const order = '1 coca cola, 5 chicken nuggets, 20 egg';
/**
 * @param {string} order
 *
 * @returns {Object}
 */
debugger
function makeOrderList() {
  let res = {};

  

  return res;
}
console.log(makeOrderList(order));
