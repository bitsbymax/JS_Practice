// import React from 'react';
import './App.scss';

/* type State = {
  x: number;
  currentDate: Date;
};

class App extends React.Component<{}, State> {
  state: State = {
    x: 1,
    currentDate: new Date(),
  };

  componentDidMount() {
    console.log('Mounted');

    setInterval(this.syncDate, 1000);
  }

  syncDate = () => {
    this.setState({
      currentDate: new Date(),
    });
  };

  render() {
    console.log('Rendering');

    return (
      <div className="App">
        <h1>React clock</h1>

        <button
          type="button"
          onClick={this.syncDate}
        >
          Update
        </button>

        <p>
          Current time:
          {` x: ${this.state.x} `}
          {this.state.currentDate.toLocaleTimeString()}
        </p>
      </div>
    );
  }
}

export default App; */
